import SwiftUI

struct StatusBarMenu: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 8) {
      Text("Deployments")
        .font(.headline)
      Text("TODO: render deployments")
        .font(.caption)
    }
    .padding(12)
  }
}
