import SwiftUI

struct NotificationSettingsView: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 8) {
      Text("Notifications")
        .font(.headline)
      Text("TODO: notification toggles")
        .font(.caption)
    }
  }
}
