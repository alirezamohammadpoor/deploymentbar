import SwiftUI

struct ProjectFilterView: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 8) {
      Text("Projects")
        .font(.headline)
      Text("TODO: list projects")
        .font(.caption)
    }
  }
}
