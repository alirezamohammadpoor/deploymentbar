// Copy to Config/Secrets.xcconfig (gitignored).
// Note: In .xcconfig, "//" starts a comment. Use the SLASH trick for URLs.
SLASH = /

VERCEL_CLIENT_ID =
VERCEL_CLIENT_SECRET =
VERCEL_REDIRECT_URI = https:$(SLASH)$(SLASH)deploymentbar.vercel.app/oauth/callback
VERCEL_SCOPES = offline_access
